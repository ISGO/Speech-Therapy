= render partial: 'menu/menu', locals: {active: "submittedFiles"}

%h2
  תרגילים משובצים
%table{:class => 'table table-hover text-center'}
  %tr
    %th.text-center id Patient
    %th.text-center Patient Name
    %th.text-center Exercise type
    %th.text-center Exercise

  - @patient_exercise.each do |patient_exercise|
  - patient = Patient.find(patient_exercise.patient_id)
    %tr
      %td= patient_exercise.patient_id
      %td= patient.firstName + ' ' + patient.lastName
      %td= patient_exercise.exercise_type
      %td= patient_exercise.exercise_id

  

%br/